{
  "name": "splycedboard-dev",
  "private": true,
  "description": "Development workspace for SplycedBoard. The product itself is the SplycedBoard/ folder.",
  "workspaces": [
    "SplycedBoard"
  ],
  "scripts": {
    "test": "node --test test/*.test.js",
    "package": "tools/package.sh",
    "check-package": "node tools/check-package.js"
  },
  "devDependencies": {
    "fast-srp-hap": "^2.0.4"
  }
}
